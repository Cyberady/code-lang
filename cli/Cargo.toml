[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
lexer = { path = "../compiler/lexer" }
parser = { path = "../compiler/parser" }
interpreter = { path = "../compiler/interpreter" }
