[package]
name = "interpreter"
version = "0.1.0"
edition = "2024"

[dependencies]
diagnostics = { path = "../diagnostics" }
parser = { path = "../parser" }
lexer = { path = "../lexer" }
rand = "0.9"
chrono = "0.4"
